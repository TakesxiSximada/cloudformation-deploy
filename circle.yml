dependencies:
  pre:
    - sh bootstrap.sh
    - make env

test:
  override:
    - make validate

deployment:
  production:
    branch: master
    commands:
      - make change-set
      - make apply

  staging:
    branch: staging
    commands:
      - make update
